# AutoKeep 开发计划 (Web & PWA 版本核心)

## 1. 核心定位
**Mobile-First 响应式 PWA (Progressive Web App)**
优先确保能够在任何设备（移动浏览器、PC浏览器）上获得一致且流畅的体验。前端框架以 **React + Vite** 为基础，UI 使用 **Tailwind CSS + Shadcn UI**。
通过统一的 Web 应用来满足前期的所有需求，后续再考虑 Capacitor 打包成原生 App。

## 2. 核心系统架构
### 前端目录结构规划
```text
/auto-keep/src
├── api              # 预留给外部 API 调用的层 (如车辆识别 API 等)
├── components       # 通用的、可复用的 UI 组件 (Shadcn UI 等)
├── hooks            # 业务与组件通用 Hook
├── pages            # 核心视图页面集合 (Auth, Dashboard, VehicleDetail 等)
├── services         # 所有的 Supabase 数据交互全部封装在此处 (与UI解耦)
├── store            # 全局状态管理 (例如：使用 Zustand 保存用户当前所在的车辆上下文)
├── utils            # 辅助工具和纯函数计算 (如：里程计算、保养过期算法逻辑)
└── App.jsx          # 路由配置入口 (目前使用 HashRouter 以适配 GitHub Pages)
```

### 数据源与后端预留
1. 数据访问层统一由 `services/db.js` 处理。不论是在哪个端，我们只需调用这个服务层的方法即可完成对 `Supabase` 的增删改查。
2. 配置好完整的环境隔离，目前所有的密钥信息维护在 `.env` 中。

## 3. 功能开发里程碑 (Milestones)

### 阶段一：Auth 鉴权与用户体系 (P0)
- [ ] 构建高转换率的美观登录 / 注册界面。
- [ ] 确保 ProtectedRoute (受保护路由) 正常拦截未登录状态的访问。
- [ ] 退出登录并清除由于登录而带入的状态数据。

### 阶段二：Dashboard 及车辆库管理 (P0)
- [ ] **多车辆管理框架**：通过 Zustand 等状态管理器保存“当前选中的车辆”。
- [ ] **车辆列表卡片展示**：显示车辆简要信息，如车牌、最近更新里程等。
- [ ] **添加新车辆**：包含表单验证及插入 Supabase 数据库 `vehicles` 表的基本流程。
- [ ] **更新车辆当前里程数**：作为使用该应用最高频的操作，UI 上需要极其简便。

### 阶段三：车辆详情面板与保养流水线 (P1)
- [ ] **保养信息流视图**：以时间线或流水单的结构展示对应选中车辆的维修/保养历史（读取 `maintenance_logs`）。
- [ ] **录入保养记录**：能够填写入新的保养维修项目、发生里程、耗费金额、具体日期和备注。
- [ ] 整合 UI 设计，提供更清晰的操作数据。

### 阶段四：智能保养提醒与算法策略 (P2 - 核心竞争力)
- [ ] 基于当前输入的车辆里程，与历史保养记录中的发生里程进行比对。
- [ ] 内置一些常见配件/项目的寿命周期字典（如：机油 10,000Km、刹车片油 40,000Km）。
- [ ] 计算出各个项目的“剩余安全里程”，对于即将超期或已超期的组件在 Dashboard 进行红色高亮预警。

### 阶段五：体验升级与交互优化 (P3)
- [ ] 引入丰富的微交互和转场动画 (Framer Motion 等)。
- [ ] 提供精美的数据驾驶舱视图，直观展示用户总计在车辆上的花费、近期的维保频率等统计图表。

## 4. 后续跨端支持 (Future)
当前代码与界面严格遵循响应式的 Mobile-First。
一旦 Web 版本完全闭环且体验达标，可通过 `npx cap init` 初始化 Capacitor，快速打包为基于 Webview 的 Android / iOS 原生应用包，且通知可由此切换至系统原生通知层。